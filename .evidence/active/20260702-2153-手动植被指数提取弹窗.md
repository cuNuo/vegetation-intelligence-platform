# 手动植被指数提取弹窗

## 当前状态

- completed

## 开始时间

- 2026-07-02 21:45

## 最近更新时间

- 2026-07-02 21:53

## 目标 / 背景

- 用户希望将右上角“批量处理”改成“植被指数提取”。
- 点击后弹出手动选择指数的弹窗，支持多选、搜索、分类筛选，并对缺失波段的指数禁用和说明原因。
- 提交后继续推送到现有任务管理器。

## 处理过程

- 在 `frontend/src/components/AssetToolbar.vue` 中新增手动指数提取弹窗状态、搜索、分类筛选、可执行性派生和多选逻辑。
- 使用 `IndexMetadata.requiredBands` 与当前 `store.asset.availableBands` 计算缺失波段，缺失时禁用该指数。
- 复用现有 `api.executeAssetBatch`，不改后端协议；任务管理器仍由全局轮询刷新。

## 验证结果

- `npm run build`：通过；保留 Vite 大 chunk 既有提示。
- Playwright MCP 页面检查：右侧入口文案已变为“植被指数提取”；无影像队列时按钮按预期禁用。
- `check_source_headers.py`：`frontend/src/components/AssetToolbar.vue` 文件头检查通过。

## 结论

- 已完成手动植被指数提取弹窗，支持指数搜索、分类筛选、多选、缺失波段禁用和任务提交。
- 无迁移，直接替换原“批量处理”入口；后端接口与任务管理器不变。

## 风险 / 后续

- 当前未通过真实影像上传路径截图验证弹窗打开后的完整视觉，因为当前浏览器会话没有影像队列；构建和状态检查已通过。
- 完整 Hook 路由仍会命中仓库既有单文件目录反模式，非本轮新增问题。
