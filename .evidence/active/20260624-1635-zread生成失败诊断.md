# zread 生成失败诊断

## 时间

- 诊断时间：2026-06-24 16:35（Asia/Shanghai）
- 仓库路径：`D:\Users\24658\Desktop\软件工程\实习`

## 操作摘要

- 检查 `zread` 后台进程，发现 2 个残留 `zread.exe` 进程，已停止。
- 检查 `C:\Users\24658\.zread\log\zread.log` 最新日志。
- 检查 `.zread/wiki/current` 与对应版本目录。
- 曾临时将 `max_concurrent` 降为 2、`max_retries` 升为 2 用于验证，诊断后已恢复原配置：`max_concurrent: 15`、`max_retries: 0`。

## 关键证据

- 最新日志区间内出现大量 `429 Too Many Requests`：
  - `429_count=794`
  - `permanent_failed_count=90`
  - `context_canceled_count=4`
- 原始并发运行阶段：
  - `page phase started, total=47 skipped=0 queued=47 workers=15 maxRetries=0`
  - 多个页面任务在 0 次 zread 层重试后永久失败。
- 降并发验证阶段：
  - `page phase started, total=41 skipped=0 queued=41 workers=2 maxRetries=2`
  - 仍出现 catalog 和 page 级别的 `429 Too Many Requests`。
- 输出目录状态：
  - `.zread/wiki/current` 指向 `versions/2026-06-24-162110`
  - `wiki.json` 中有 47 个目录页面条目
  - 对应版本目录下 Markdown 页面文件数量为 0，缺失页面 47 个

## 结论

本次失败的直接原因是 LLM 提供方返回 `429 Too Many Requests` 限流，导致 zread 页面生成任务批量失败。降低并发到 2 后仍失败，说明当前额度、限速窗口或服务端策略仍不足以支撑连续生成。`context canceled` 是后续中断或进程停止导致的伴随结果，不是首要根因。

当前 `.zread/wiki` 只形成了目录索引，没有形成可用正文页面，因此不能视为完整生成成功。

## 后续建议

1. 先等待服务端限流窗口恢复，再以更低并发重新生成。
2. 将 zread 全局配置临时调为 `max_concurrent: 1`、`max_retries: 3`，并避免同时启动多个 zread。
3. 若继续 429，换用更高额度或更稳定限流策略的 LLM provider。
