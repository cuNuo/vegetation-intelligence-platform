# 任务日志：bajiepart1 真实数据端到端验证

## 任务时间

- 开始时间：2026-07-02 10:16
- 最近更新时间：2026-07-02 10:55
- 当前状态：已完成

## 任务目标

- 使用 `data/test/bajiepart1.tif` 验证上传、波段映射、Agent 计划、任务提交、结果展示和多指数切换是否正常。
- 检查用户反馈的上传进度、对话底部固定、计算结果显示、结果统计切换、波段自动映射和手动修正入口。

## 输入与关键事实

- 测试数据：`D:\Users\24658\Desktop\软件工程\实习\data\test\bajiepart1.tif`
- 影像大小：510,213,751 bytes。
- 元数据检查结果：
  - 宽高：6376 x 3194。
  - 波段数：6。
  - CRS：EPSG:32650。
  - `descriptions` 全部为空。
  - `bandMetadata[*].wavelengthNm` 全部为空。
- 结论：该测试影像本身不提供中心波长或可识别波段描述，无法真正按波长自动映射；前端必须明确提示“无波长元数据，按常见顺序兜底”，手动修正作为保底。

## 本轮修复

- `frontend/src/components/AssetToolbar.vue`
  - 将波段映射保留为点击“逻辑波段”后的弹窗，不再常驻展开。
  - 弹窗展示源波段证据：波长、描述或“无波长或描述元数据”。
  - 上传进度显示文件名、百分比和总进度条。
  - 修复资产工具栏层级被地图顶部浮层截获点击的问题。
- `frontend/src/composables/usePlatformApi.ts`
  - 上传改为 XHR 进度回调，并增强非 JSON 错误响应解析。
- `frontend/src/stores/workspace.ts`
  - 保持结果组模型，支持同一任务多个指数产品切换。
  - 波段映射优先级为：波长元数据 -> 描述关键词 -> 常见顺序兜底 -> 手动修正。
- `backend/app/services/assets.py`
  - `inspect_raster()` 返回 `bandMetadata`，包含 `band / description / tags / wavelengthNm`。
- `backend/tests/test_api.py`
  - 上传接口测试新增 `bandMetadata` 结构断言。

## 真实后端计算验证

- 调用方式：使用 FastAPI TestClient 调用 `/processes/batch/execution`。
- 输入指数：`ndvi / evi / gndvi`。
- 输入映射：
  - `blue=1`
  - `green=2`
  - `red=3`
  - `red_edge=5`
  - `nir=4`
  - `swir1=6`
  - `swir2=0`
- 结果：
  - HTTP 200。
  - 引擎：numpy。
  - 耗时：约 25.9 秒。
  - NDVI/EVI/GNDVI 输出 COG 均存在，预览图均存在。
  - 统计均成功生成，均值分别约为 NDVI 0.8969、EVI 0.7467、GNDVI 0.8027。

## 浏览器端到端验证

- 启动状态：
  - 后端：`http://127.0.0.1:8011/health` 返回 healthy。
  - 前端：Vite dev server 可访问。
- Playwright 验证脚本：`output/playwright/validate-ui.cjs`。
- 验证数据：`data/test/bajiepart1.tif`。
- 验证结果：
  - 上传进度采样正常，例如 `2% / 7% / 11% / 16% / 20% / 25% / 29% / 32%`。
  - 上传完成后显示 `已导入 1 个影像，可执行批量处理`。
  - “逻辑波段”点击可打开弹窗，且不再被地图浮层截获。
  - 弹窗提示 `6 个源波段，无波长元数据，按常见顺序兜底`。
  - 实际映射值：
    - Blue -> 1
    - Green -> 2
    - Red -> 3
    - Red Edge -> 5
    - NIR -> 4
    - SWIR 1 -> 6
    - SWIR 2 -> 0
  - Agent 计划包含 NDVI/EVI/GNDVI 三个指数。
  - 确认提交后任务 successful。
  - 统计面板可切换 NDVI/EVI/GNDVI，最终切换到 GNDVI 后显示均值 0.8027、标准差 0.0645、有效像元 20,364,436。
  - 对话输入区在验证视口下保持可见，底部位置未被新消息挤出。
- 浏览器控制台仅出现 WebGL `ReadPixels` 性能警告，不影响业务功能。

## 质量检查

- `D:\miniconda\envs\giskeshe\python.exe -m ruff check backend/app backend/tests`
  - 通过。
- `D:\miniconda\envs\giskeshe\python.exe -m pytest backend/tests/test_api.py backend/tests/test_agent.py -q`
  - 23 passed，1 个 Starlette TestClient deprecation warning。
- `npm run build`
  - 通过，仅保留 Vite chunk size warning。

## 证据产物

- `output/playwright/band-mapping-modal.png`
- `output/playwright/multi-index-results.png`
- `output/playwright/validate-ui.cjs`
- `.evidence/runtime-logs/20260702-frontend-dev.log`
- `.evidence/runtime-logs/20260702-frontend-dev.err.log`

## 风险与后续

- 当前测试影像没有波长元数据，因此不能证明“按波长自动映射”在有波长标签的影像上命中，只能证明系统能读取波长字段、无波长时明确降级并允许手动修正。
- 若后续要更强验证，建议补一份带 `wavelength`、`central_wavelength` 或 `BandName` 标签的 GeoTIFF 测试数据，覆盖波长优先路径。
- Vite 构建仍有大 chunk 警告，属于性能优化项，不影响本轮功能正确性。
